<form method="post" action="{{ path('etudiant_delete', {'id': etudiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.id) }}">
    <button class="pd-setting-ed btn" data-toggle="tooltip" title="Supprimer">
                                                        <i aria-hidden="true" class="fa fa-trash-o" href="{{ path('etudiant_edit', {'id': etudiant.id}) }}"></i>
                                                    </button>
</form>
